name: Audit

on:
  push:
    branches: [main]
    paths:
      - "**/Cargo.toml"
      - "Cargo.lock"
      - "deny.toml"
      - ".github/workflows/audit.yml"
  pull_request:
    branches: [main]
    paths:
      - "**/Cargo.toml"
      - "Cargo.lock"
      - "deny.toml"
      - ".github/workflows/audit.yml"
  schedule:
    # Catches new advisories between pushes. GitHub disables scheduled
    # workflows after 60 days of repo inactivity — check this is still
    # running as part of the release checklist.
    - cron: "0 6 * * 1"

env:
  CARGO_TERM_COLOR: always

jobs:
  cargo-deny:
    name: cargo deny check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          # The action's own default passes --all-features, which overrides
          # deny.toml's `[graph] all-features = false` and would silently
          # diverge from what `cargo deny check` does locally. Empty string
          # makes CI match the documented local repro command exactly.
          arguments: ""
